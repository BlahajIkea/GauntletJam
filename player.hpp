#include "raylib.h"

class Player {

    public:
    Vector2 playerPos;

    bool isAlive;
    bool movingUp;
    bool movingDown;
    bool isSprinting;

    bool isFacingDown;
    bool isFacingUp;

    float playerMoveSpeed = 4;
    float playerSprintSpeed = 4;


    private:
    
};